

<h1><%= @garden.name %>
  <% if @garden.user == current_user  %>
    ( <%= link_to "Edit", edit_garden_path(@garden.id) %> | <%= link_to 'Destroy', @garden, method: :delete, data: { confirm: 'Are you sure you want to delete this garden?' } %> )
  <% end %>  
  </h1>

<h2> Description: <%= @garden.description %> </h2>

<h2> Square Feet: <%= @garden.square_feet %> </h2>

Add plantings

Add edit link
Add something about user
