
<%= display_any_errors(@species) %>

<div class="ui container center aligned">

    <div>

        <h1 class="underlined"> Edit <%= @species.name %> </h1>

        <div class="ui divider divider-spacer"></div>

        <div class="ui big form center aligned">

                <%= render partial: "species/form", locals: { species: @species } %>

            </div>

            <p class="sub-menu-item bold"> <%= link_to "Or Delete Species", user_species_path(current_user.id, @species.id), method: :delete, data: { confirm: 'Are you sure you want to delete this species?' }%> </p>


        </div>

</div>
